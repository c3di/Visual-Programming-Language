describe('pythonGenerator.programToCode', () => {
  beforeEach(() => {});

  test('generate the code ', () => {
    expect(2 - 2).toBe(0);
  });

  test('generate the code from the program', () => {});
});
export {};
