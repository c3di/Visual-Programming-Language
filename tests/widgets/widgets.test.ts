// todo 'str' and "str" for text inputs

// multiple line string for text inputs
describe('Visual Program Class', () => {
  beforeEach(() => {});

  test('generate the code ', () => {
    expect(2 - 2).toBe(0);
  });
});
export {};
